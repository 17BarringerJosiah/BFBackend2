spring:
  application:
    name: bfbackend2

  datasource:
    url: jdbc:postgresql://localhost:5434/bfbackend2
    driver-class-name: org.postgresql.Driver
    username: bfbackend2
    password: bfbackend2
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080